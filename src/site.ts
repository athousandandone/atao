// Site-level values, introduced at first use (Slice 4). The byline is
// site configuration until multi-author work exists; the origin and
// title values join in Slice 5 where feeds and metadata require them.

export const byline = 'Oliver Matthews';
